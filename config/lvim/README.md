# LunarVim Memory Muscle

## Quick Patterns

*Document your keybindings and workflows here*

## Customizing LunarVim

```bash
# Edit in normal location (it's symlinked)
cd ~/.config/lvim
# edit config.lua

# Commit from ~/.me
cd ~/.me
git add config/lvim/config.lua
git commit -m "Customized lvim keybinding"
```

## Integration with Claude

*How you use LunarVim alongside Claude sessions*
